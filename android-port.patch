diff -ruN orig/CMakeLists.txt new/CMakeLists.txt
--- orig/CMakeLists.txt	2026-09-18 18:14:00.373740144 +0000
+++ new/CMakeLists.txt	2026-09-18 18:14:28.447594003 +0000
@@ -47,6 +47,14 @@
     return()
 endif()
 
+# Android build. cmake/android.cmake (used with the NDK toolchain, which
+# defines ANDROID) builds libmain.so for SDL's SDLActivity and return()s, same
+# shape as the PS2/Wii branches above.
+if(ANDROID)
+    include(${CMAKE_CURRENT_SOURCE_DIR}/cmake/android.cmake)
+    return()
+endif()
+
 enable_testing()
 
 function(opticraft_add_optional_test target test_source)
@@ -164,7 +172,7 @@
 # Build the desktop target from common code plus the PC tree. Foreign console
 # sources are never discovered, so adding a new Wii/PS2 .cpp cannot silently
 # enter a desktop link.
-opticraft_collect_platform_sources(OPTICRAFT_COMMON_SOURCES pc)
+mcbeta_collect_platform_sources(OPTICRAFT_COMMON_SOURCES pc)
 set(OPTICRAFT_MINIZIP_SOURCES
     "${CMAKE_CURRENT_SOURCE_DIR}/external/zlib/contrib/minizip/ioapi.c"
     "${CMAKE_CURRENT_SOURCE_DIR}/external/zlib/contrib/minizip/unzip.c"
@@ -175,15 +183,15 @@
 )
 
 if(WIN32)
-    opticraft_exclude_sources(OPTICRAFT_COMMON_SOURCES "[/\\]pc[/\\]linux[/\\]")
+    mcbeta_exclude_sources(OPTICRAFT_COMMON_SOURCES "[/\\]pc[/\\]linux[/\\]")
 elseif(UNIX)
-    opticraft_exclude_sources(OPTICRAFT_COMMON_SOURCES "[/\\]pc[/\\]win32[/\\]")
+    mcbeta_exclude_sources(OPTICRAFT_COMMON_SOURCES "[/\\]pc[/\\]win32[/\\]")
 else()
     message(FATAL_ERROR "OptiCraft currently supports Windows and Linux only")
 endif()
 
 # Each public platform facade has exactly one implementation in this target.
-opticraft_select_platform_backends(OPTICRAFT_COMMON_SOURCES PC PC PC)
+mcbeta_select_platform_backends(OPTICRAFT_COMMON_SOURCES PC PC PC)
 
 # Unified diagnostic verbosity, the same knob cmake/ps2.cmake and cmake/wii.cmake
 # expose. The desktop target left it undefined, so platform/Log.h fell back to 0
diff -ruN orig/src/pc/Main.cpp new/src/pc/Main.cpp
--- orig/src/pc/Main.cpp	2026-09-18 18:14:00.378228758 +0000
+++ new/src/pc/Main.cpp	2026-09-18 18:14:28.447746429 +0000
@@ -1,4 +1,6 @@
+#ifndef __ANDROID__
 #define SDL_MAIN_HANDLED
+#endif
 #include "SDL.h"
 
 #include <cctype>
diff -ruN orig/src/pc/linux/java/File.cpp new/src/pc/linux/java/File.cpp
--- orig/src/pc/linux/java/File.cpp	2026-09-18 18:14:00.395865867 +0000
+++ new/src/pc/linux/java/File.cpp	2026-09-18 18:14:28.448343506 +0000
@@ -23,6 +23,9 @@
 #endif
 
 #include "util/Memory.h"
+#if defined(__ANDROID__)
+#include <SDL.h>
+#endif
 
 static std::string ToPath(const jstring &path)
 {
@@ -235,6 +238,10 @@
 File *File::openWorkingDirectory(const jstring &name)
 {
 	// Get the home directory environment variable
+#if defined(__ANDROID__)
+	if (const char *external = SDL_AndroidGetExternalStoragePath())
+		return new File_Impl(FromPath(external) + "/" + name);
+#endif
 	const char *path = ::getenv("HOME");
 	if (path == nullptr)
 		return new File_Impl("");
diff -ruN orig/src/pc/lwjgl/GLContext.cpp new/src/pc/lwjgl/GLContext.cpp
--- orig/src/pc/lwjgl/GLContext.cpp	2026-09-18 18:14:00.382565324 +0000
+++ new/src/pc/lwjgl/GLContext.cpp	2026-09-18 18:14:28.447853159 +0000
@@ -13,6 +13,9 @@
 #endif
 
 #include "SDL.h"
+#if defined(__ANDROID__)
+#include <gl4esinit.h>
+#endif
 
 // #define MC_DEBUG_GL
 
@@ -174,9 +177,16 @@
 
 		pcRenderBackendSetActive(PcRenderBackendType::OpenGL);
 
+#if defined(__ANDROID__)
+		// gl4es translates the fixed-function GL 1.x calls onto this GLES 2.0 context.
+		SDL_GL_SetAttribute(SDL_GL_CONTEXT_MAJOR_VERSION, 2);
+		SDL_GL_SetAttribute(SDL_GL_CONTEXT_MINOR_VERSION, 0);
+		SDL_GL_SetAttribute(SDL_GL_CONTEXT_PROFILE_MASK, SDL_GL_CONTEXT_PROFILE_ES);
+#else
 		SDL_GL_SetAttribute(SDL_GL_CONTEXT_MAJOR_VERSION, 1);
 		SDL_GL_SetAttribute(SDL_GL_CONTEXT_MINOR_VERSION, 1);
 		SDL_GL_SetAttribute(SDL_GL_CONTEXT_PROFILE_MASK, SDL_GL_CONTEXT_PROFILE_COMPATIBILITY);
+#endif
 		SDL_GL_SetAttribute(SDL_GL_DEPTH_SIZE, 24);
 		SDL_GL_SetAttribute(SDL_GL_DOUBLEBUFFER, 1);
 
@@ -224,8 +234,17 @@
 		if (window == nullptr || gl_context == nullptr)
 			throw SDLException();
 
+#if defined(__ANDROID__)
+		// gl4es is linked statically with NOX11/NOEGL: SDL owns the EGL context,
+		// gl4es only needs to know how to look up the GLES entry points.
+		set_getprocaddress(SDL_GL_GetProcAddress);
+		initialize_gl4es();
+		if (!gladLoadGLLoader(reinterpret_cast<GLADloadproc>(gl4es_GetProcAddress)))
+			throw std::runtime_error("Failed to load glad");
+#else
 		if (!gladLoadGLLoader(reinterpret_cast<GLADloadproc>(SDL_GL_GetProcAddress)))
 			throw std::runtime_error("Failed to load glad");
+#endif
 
 		SDL_GL_SetSwapInterval(0);
 
diff -ruN orig/src/pc/render/PcOpenGlRenderBackend.cpp new/src/pc/render/PcOpenGlRenderBackend.cpp
--- orig/src/pc/render/PcOpenGlRenderBackend.cpp	2026-09-18 18:14:00.387082415 +0000
+++ new/src/pc/render/PcOpenGlRenderBackend.cpp	2026-09-18 18:14:28.448022968 +0000
@@ -55,11 +55,19 @@
     return major > 1 || (major == 1 && minor >= 2);
 }
 
+#if defined(__ANDROID__)
+#include <gl4esinit.h>
+// Extension/core entry points must come from gl4es, not from the raw GLES driver.
+#define PC_GL_PROC(name) gl4es_GetProcAddress(name)
+#else
+#define PC_GL_PROC(name) SDL_GL_GetProcAddress(name)
+#endif
+
 // SDL_GL_GetProcAddress-based runtime loaders (desktop only).
 static void *getProcEither(const char *arbName, const char *coreName)
 {
-	void *f = SDL_GL_GetProcAddress(arbName);
-	if (!f) f = SDL_GL_GetProcAddress(coreName);
+	void *f = PC_GL_PROC(arbName);
+	if (!f) f = PC_GL_PROC(coreName);
 	return f;
 }
 
diff -ruN orig/src/platform/Resources_PC.cpp new/src/platform/Resources_PC.cpp
--- orig/src/platform/Resources_PC.cpp	2026-09-18 18:14:00.391455966 +0000
+++ new/src/platform/Resources_PC.cpp	2026-09-18 18:14:28.448240031 +0000
@@ -8,6 +8,12 @@
 
 std::string PlatformResources::baseDir()
 {
+#if defined(__ANDROID__)
+    // No exe directory on Android: assets.pak, .minecraft/ and username.txt live in
+    // the app's external files dir (/sdcard/Android/data/<package>/files/).
+    if (const char* external = SDL_AndroidGetExternalStoragePath())
+        return std::string(external) + "/";
+#endif
     char* base = SDL_GetBasePath();
     if (base)
     {
